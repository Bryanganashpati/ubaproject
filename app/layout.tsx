import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "IrsyadJobdesk",
  description: "Saya Owner Resmi sekaligus pemilik dari Agency Munjaya Group secara Sah dan Resmi tujuan saya membangun agency ini untuk membantu pelajar bisa mandiri mendapatkan hasil dari kerjanya.",
  alternates: {
    canonical: "https://irsyadjobdesk.vercel.app",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="id">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
